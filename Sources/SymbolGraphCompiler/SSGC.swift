/// The namespace for the **s**tatic **s**ymbol **g**raph **c**ompiler.
///
/// Alternatively, it could stand for **s**wift **s**ymbol **g**raph **c**ompiler. However, we
/// have two symbol graph linkers (the static linker and the dynamic linker), so officially the
/// *s* stands for *static*.
@frozen public enum SSGC {
}
