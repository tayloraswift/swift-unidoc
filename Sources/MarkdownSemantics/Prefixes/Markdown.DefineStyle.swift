extension Markdown
{
    enum DefineStyle
    {
        /// Definition will be rendered with text styling.
        case text
        /// Definition will be rendered as inline code.
        case code
    }
}
